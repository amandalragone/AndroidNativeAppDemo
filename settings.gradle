pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
    }
}
rootProject.name = "AndroidNativeAppDemo"
include ':app'
include ':EndeavorAndroidERNContainer'
project(':EndeavorAndroidERNContainer').projectDir = new File('endeavor-android-native-app-container/lib')